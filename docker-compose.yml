services:
  db:
    image: mysql
    container_name: lionz-db
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: lionz_db
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    restart: unless-stopped

volumes:
  mysql-data:
